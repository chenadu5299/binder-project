===========================================
🚀 DeepSeek API Key 快速配置
===========================================

方法 1：通过应用界面（最简单）⭐
--------------------------------
1. 在欢迎界面点击「配置 API Key（使用 AI 功能）」按钮
2. 输入 API key: sk-de7beef46f714ecfaa511acc98d9294a
3. 点击「保存 DeepSeek Key」
4. 完成！

方法 2：通过控制台（一键执行）
--------------------------------
打开开发者工具（Cmd+Option+I），切换到 Console，然后：

【完整代码，直接复制粘贴执行】

(async () => {
    const { invoke } = await import('@tauri-apps/api/core');
    await invoke('ai_save_api_key', {
        provider: 'deepseek',
        key: 'sk-de7beef46f714ecfaa511acc98d9294a'
    });
    console.log('✅ DeepSeek API key 配置成功！');
})();

===========================================
