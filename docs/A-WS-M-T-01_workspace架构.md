# workspace架构

## 文档头

- 结构编码：`WS-M-T-01`
- 文档属性：`主结构`
- 主责模块：`WS`
- 文档职责：`workspace架构 / 模型、架构与机制主控`
- 上游约束：`CORE-C-D-04`, `SYS-C-T-01`, `WS-C-T-01`, `WS-C-T-02`, `WS-C-T-03`, `WS-M-D-01`
- 直接承接：无
- 接口耦合：`AST-M-P-01`, `AG-M-P-01`, `SYS-I-P-01`
- 汇聚影响：`CORE-C-R-01`, `WS-C-T-01`, `WS-M-D-01`
- 扩散检查：`WS-M-T-02`, `WS-M-T-03`, `WS-M-T-04`, `WS-M-T-05`
- 使用边界：`定义技术模型、实现约束与关键机制，不承担产品边界裁定与排期管理`
- 变更要求：`修改本文后，必须复核：上游约束、直接承接、接口耦合、汇聚影响、扩散检查文档`

---
## 一、文档定位

本文定义 Binder MVP 的 Workspace 架构，统一对象模型、核心主链、状态边界和对外契约。  
本文是 Workspace 子系统架构文档，不替代资源管理细节实现文档。

补充定位：

Workspace 在 Binder 中不是普通子模块。  
它是 Binder 的基础工作框架，承担“工作区锚定、资源进入、任务承载、结果回流”的底座职责。  
其他能力通常不是与 Workspace 并列再造一套工作面，而是在 Workspace 框架之上完成工作分发与结果呈现。

---

## 二、架构目标

1. 提供稳定的本地工作区容器（文件、资源、上下文的一致管理）。  
2. 支撑“文件管理 -> 文档编辑 -> AI 调用 -> 结果生效”的连续主链。  
3. 保证跨文档与未打开文档场景下的可执行与可恢复。  
4. 让 Workspace 成为 Binder 其他能力进入系统、消费资源、回流结果的统一工作底座。  

---

## 三、Workspace 对象模型

### 3.0 基础定位

Workspace 不只是“装文件的容器”，还承担以下基础职责：

1. 锚定当前工作的作用域。
2. 为文件、资源、上下文和任务提供统一归属。
3. 承接 AI、编辑器、跨文档协作的共同工作底座。

因此：

1. 其他能力可以在 Workspace 内协同，但不应在 Workspace 外再复制一套平行工作台。
2. 任何需要读取、引用、编辑、沉淀工作区资产的能力，都应先回到 Workspace 语义下定义边界。

## 3.1 核心对象

1. Workspace  
本地项目容器，包含路径、索引、缓存、会话上下文。  

2. WorkspaceFile  
工作区内文件对象，包含 filePath、fileType、mtime、缓存状态。  

3. FileCache  
文件缓存对象，存储 canonical HTML（或等价可编辑快照）与版本信息。  

4. PendingDiffSet  
文件级待确认修改集合，支持按文件与会话追踪。  

5. DependencyLink  
文件间引用/依赖关系（用于上下文注入与影响分析）。  

## 3.2 对象关系

1. 一个 Workspace 包含多个 WorkspaceFile。  
2. 每个 WorkspaceFile 对应一个 FileCache（按版本更新）。  
3. PendingDiffSet 绑定 WorkspaceFile。  
4. DependencyLink 在 Workspace 作用域内建立文件关系图。  

---

## 四、核心主链

## 4.1 打开文件主链

1. 解析目标文件路径。  
2. 优先读取 FileCache（命中则直接返回快照）。  
3. 未命中则读盘并规范化，生成缓存后返回。  
4. 编辑器加载快照并建立 tab 绑定。  

## 4.2 保存文件主链

1. 编辑器提交内容。  
2. 写盘成功后同步更新 FileCache。  
3. 更新 mtime 与版本状态。  
4. 通知依赖链和相关视图刷新。  

## 4.3 未打开文件编辑主链

1. 通过 Workspace 服务加载目标文件快照。  
2. 生成 pending diff（不直接写盘）。  
3. 用户确认后再执行落盘。  
4. 更新缓存与状态。  

## 4.4 外部修改处理主链

1. 监听文件 mtime 变化。  
2. 检测冲突后触发外部修改处理流程。  
3. 选择加载外部内容时，关联 pending diff 进入失效流。  
4. 保持主链可继续，不阻断其他文件操作。  

---

## 五、分层结构（Workspace 子系统内）

1. 接口层（Commands/API）  
对前端暴露 open/save/cache/diff 相关命令。  

2. 领域层（Workspace Domain）  
管理 WorkspaceFile、FileCache、PendingDiffSet、DependencyLink 规则。  

3. 存储层（workspace.db + file system）  
持久化缓存、pending diff 元数据、依赖关系与状态索引。  

---

## 六、状态与一致性约束

1. 同一文件只允许一个当前有效缓存快照。  
2. 写盘成功后必须同步更新缓存，禁止“磁盘新、缓存旧”。  
3. pending diff 不等于已生效内容。  
4. 外部修改与手动修改触发的失效语义保持一致。  
5. 未打开文件场景必须先完成就绪链再进入编辑执行。  

---

## 七、对外契约（与其他子系统）

1. 对编辑器子系统  
提供文件打开、缓存加载、保存回写、外部修改通知。  

2. 对 AI 执行子系统  
提供目标文件快照读取、未打开文件上下文准备、pending diff 容器。  

3. 对对话编辑子系统  
提供按文件维度的 diff 池与确认后落盘能力。  

4. 对可观测子系统  
输出关键事件：缓存命中、缓存回写、冲突处理、批量确认结果。  

### 7.1 Workspace 作为工作分发底座

在 Binder 的整体架构中：

1. 编辑器是在 Workspace 内消费和编辑文件。  
2. AI 是在 Workspace 内读取资源、形成建议、回流结果。  
3. 对话编辑是在 Workspace 内围绕目标文件和资源集合进行受控执行。  
4. 记忆、知识库、模板库等资产系统，也是在 Workspace 语义下暴露入口、建立边界和完成协同。

换言之，Workspace 不是“若干子系统之一的附属背景”，而是其他子系统进行工作分发和结果呈现的共同基础层。

---

## 八、MVP 验收口径

1. Workspace 打开/切换稳定，不出现路径错位。  
2. 文件打开与保存后缓存一致。  
3. 未打开文件可进入 pending diff 主链并确认落盘。  
4. 外部修改冲突可处理且状态一致。  
5. 与编辑器/AI/对话编辑三方交互无协议断裂。  

---

## 九、与 004/005/006 的关系

1. ``A-WS-M-T-01_workspace架构.md`` 是系统架构层的 Workspace 子系统视角。  
2. `A-WS-C-T-01_workspace模型定义.md` 负责对象定义与边界。  
3. `A-WS-C-T-02_workspace对象生命周期.md` 负责状态流转。  
4. `A-WS-C-T-03_文档资源任务关系模型.md` 负责关系与任务链映射。  

---

## 十、关联文档

1. `A-SYS-C-T-01_系统总体架构.md`  
2. `A-WS-C-T-01_workspace模型定义.md`  
3. `A-WS-C-T-02_workspace对象生命周期.md`  
4. `A-WS-C-T-03_文档资源任务关系模型.md`  
5. `R-WS-M-R-02_Workspace改造可落地实施方案.md`  
6. `R-WS-M-R-01_资源管理需求文档.md`  