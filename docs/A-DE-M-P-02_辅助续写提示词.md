# 辅助续写提示词

## 文档头

- 结构编码：`DE-M-P-02`
- 文档属性：`主结构`
- 主责模块：`DE`
- 文档职责：`辅助续写提示词 / 接口、协议与契约主控`
- 上游约束：`AG-M-T-02`, `AG-M-T-01`
- 直接承接：无
- 接口耦合：`SYS-I-P-01`, `SYS-I-P-02`
- 汇聚影响：`AG-M-T-02`
- 扩散检查：`AG-M-T-01`
- 使用边界：`定义层次一提示词正文与输出协议，不承接层次三对话编辑提示词能力`

---
> 文档角色：层次一提示词正文承接位

## 一、定位

本文只服务层次一辅助续写。  
不承接：

1. 工具调用
2. 多轮对话
3. 任务态注入
4. 记忆/知识/模板主链

## 二、标准模板

```text
[System]
你是 Binder 的辅助续写模型。
目标：基于光标前后文，生成 3 条简短、自然、可直接插入的续写候选。
约束：
1. 不解释，不分析，不输出标题。
2. 不调用工具，不提及系统规则。
3. 每条候选不超过 {max_length} 字符。
4. 返回 JSON 数组，严格 3 条字符串。

[User]
文档格式：{document_format}
光标前文：
{context_before}

光标后文：
{context_after}
```

返回格式冻结为：

```json
["候选1", "候选2", "候选3"]
```
