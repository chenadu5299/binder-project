# Binder（合页）第三阶段开发计划

## 文档信息

- **阶段名称**：AI 功能集成与优化
- **开发周期**：6 周（Week 11-16）
- **开发目标**：实现完整的 AI 三层架构，包括自动补全、Inline Assist、聊天窗口，以及 SQLite 全文索引和记忆库功能
- **版本**：v3.0（基于前两阶段经验优化版）
- **创建日期**：2025年
- **最后更新**：2025年
- **更新说明**：基于第一阶段和第二阶段的经验教训，重点关注稳定性、API 验证、错误处理和测试覆盖

## 总体目标

实现完整的 AI 辅助功能，包括三层架构（自动补全、Inline Assist、聊天窗口）、SQLite 全文索引、记忆库功能，为 Binder 提供强大的 AI 能力。

**核心原则**（基于前两阶段经验）：
- ✅ **稳定性优先**：每个功能都要有完整的错误处理、重试机制和降级方案
- ✅ **API 提前验证**：所有新 API 使用前都要验证，特别是 Tauri 2.x 和 AI 服务 API
- ✅ **性能优化**：请求缓存、去重、取消机制，避免资源浪费
- ✅ **用户体验**：流畅的交互、清晰的反馈、优雅的错误提示
- ✅ **测试覆盖**：关键功能要有单元测试和集成测试
- ✅ **零盲点**：功能的每个环节、流程的每个逻辑链都要有明确的设计

---

## 前两阶段经验总结

### ✅ 成功经验

1. **模块化设计**：前后端分离清晰，易于维护和测试
2. **类型安全**：TypeScript 严格模式，减少运行时错误
3. **状态管理**：Zustand 使用简单高效，状态管理清晰
4. **文件系统监听**：使用 notify crate 实现自动刷新
5. **错误处理**：完善的错误处理和用户提示
6. **布局优化**：固定宽度布局，标签栏横向滚动

### ⚠️ 需要改进

1. **API 兼容性**：Tauri 2.x API 变化需要提前验证
2. **权限配置**：需要详细的权限配置文档
3. **测试覆盖**：缺少测试，导致问题发现较晚
4. **性能优化**：某些操作（如文件树构建）需要优化
5. **用户体验**：某些交互（如标签栏滚动）需要改进

### 🔧 改进措施

1. **提前验证 API**：每个新 API 使用前都要验证，特别是 AI 服务 API
2. **详细权限文档**：列出所有需要的权限和配置方法
3. **完善错误处理**：所有关键操作都要有错误处理、重试机制和降级方案
4. **增加测试**：关键功能要有单元测试和集成测试
5. **性能优化**：请求缓存、去重、取消机制，避免资源浪费
6. **用户体验**：流畅的交互、清晰的反馈、优雅的错误提示

---

## Week 11-12：AI 服务基础架构

### 目标

搭建 AI 服务基础架构，包括模型管理、请求队列、错误处理和流式响应支持。

### 11.1 AI 服务架构设计（Day 1-2）

#### 11.1.1 后端 AI 服务框架

**任务**：创建 AI 服务基础架构

**具体步骤**：

1. **创建 AI 服务模块结构**
   ```
   src-tauri/src/services/
   ├── ai_service.rs          # AI 服务主模块
   ├── ai_providers/          # AI 提供商实现
   │   ├── mod.rs
   │   ├── openai.rs          # OpenAI 实现
   │   ├── anthropic.rs       # Anthropic 实现
   │   ├── gemini.rs          # Google Gemini 实现
   │   └── local.rs           # 本地模型实现（Ollama）
   ├── ai_queue.rs            # AI 请求队列
   └── ai_config.rs           # AI 配置管理
   ```

2. **定义 AI 服务接口**
   ```rust
   // src-tauri/src/services/ai_service.rs
   pub trait AIProvider: Send + Sync {
       async fn autocomplete(&self, context: &str) -> Result<String, AIError>;
       async fn inline_assist(&self, instruction: &str, text: &str) -> Result<String, AIError>;
       async fn chat_stream(&self, messages: &[Message]) -> Result<Stream, AIError>;
   }
   
   pub struct AIService {
       providers: HashMap<String, Box<dyn AIProvider>>,
       queue: AIRequestQueue,
       config: AIConfig,
   }
   ```

3. **实现 AI 配置管理**
   ```rust
   // src-tauri/src/services/ai_config.rs
   #[derive(Debug, Clone, Serialize, Deserialize)]
   pub struct AIConfig {
       pub request_timeout: Duration,           // 默认 60 秒
       pub autocomplete_trigger_delay: Duration, // 默认 7 秒（5-15 秒范围）
       pub undo_redo_max_steps: usize,         // 默认 50
       pub max_concurrent_requests: usize,     // 默认 3
   }
   
   impl AIConfig {
       pub fn load() -> Result<Self, String> {
           // 从配置文件加载，如果不存在则使用默认值
       }
       
       pub fn save(&self) -> Result<(), String> {
           // 保存到配置文件
       }
   }
   ```

**验收标准**：
- ✅ AI 服务模块结构创建完成
- ✅ AI 服务接口定义完成
- ✅ AI 配置管理实现完成
- ✅ 代码编译通过

#### 11.1.2 AI 请求队列实现

**任务**：实现 AI 请求队列，支持优先级和并发控制

**具体步骤**：

1. **实现请求队列**
   ```rust
   // src-tauri/src/services/ai_queue.rs
   #[derive(Debug, Clone, PartialEq, Eq, PartialOrd, Ord)]
   pub enum RequestPriority {
       Low,      // 自动补全
       Normal,   // 聊天
       High,     // Inline Assist
   }
   
   pub struct AIRequest {
       pub id: String,
       pub priority: RequestPriority,
       pub request_type: RequestType,
       pub cancel_token: CancellationToken,
   }
   
   pub struct AIRequestQueue {
       queue: VecDeque<AIRequest>,
       active_requests: usize,
       max_concurrent: usize,
   }
   
   impl AIRequestQueue {
       pub async fn enqueue(&mut self, request: AIRequest) -> Result<()>;
       pub async fn dequeue(&mut self) -> Option<AIRequest>;
       pub fn cancel(&mut self, request_id: &str) -> bool;
   }
   ```

2. **实现请求取消机制**
   - 使用 `tokio::sync::CancellationToken` 实现请求取消
   - 用户继续输入时自动取消自动补全请求
   - 用户关闭标签页时取消所有相关请求

**验收标准**：
- ✅ 请求队列实现完成
- ✅ 优先级管理实现完成
- ✅ 并发控制实现完成
- ✅ 请求取消机制实现完成
- ✅ 单元测试通过

#### 11.1.3 AI 错误处理

**任务**：实现完善的 AI 错误处理和重试机制

**具体步骤**：

1. **定义错误类型**
   ```rust
   // src-tauri/src/services/ai_service.rs
   #[derive(Debug, Clone, Serialize, Deserialize)]
   pub enum AIError {
       NetworkError(String),
       RateLimit { retry_after: u64 },
       ModelUnavailable,
       ContextTooLong,
       Timeout,
       Cancelled,
       Unknown(String),
   }
   
   impl AIError {
       pub fn is_retryable(&self) -> bool {
           matches!(
               self,
               AIError::NetworkError(_) 
               | AIError::RateLimit { .. } 
               | AIError::ModelUnavailable
           )
       }
       
       pub fn retry_after(&self) -> Option<u64> {
           match self {
               AIError::RateLimit { retry_after } => Some(*retry_after),
               _ => None,
           }
       }
   }
   ```

2. **实现重试机制**
   - 可重试错误自动重试（最多 3 次）
   - 使用指数退避策略
   - RateLimit 错误等待指定时间后重试

**验收标准**：
- ✅ 错误类型定义完成
- ✅ 重试机制实现完成
- ✅ 错误处理测试通过

### 11.2 OpenAI 提供商实现（Day 3-5）

#### 11.2.1 OpenAI API 集成

**任务**：实现 OpenAI 提供商

**具体步骤**：

1. **添加依赖**
   ```toml
   # src-tauri/Cargo.toml
   [dependencies]
   reqwest = { version = "0.11", features = ["json", "stream"] }
   tokio-stream = "0.1"
   serde_json = "1.0"
   ```

2. **实现 OpenAI 提供商**
   ```rust
   // src-tauri/src/services/ai_providers/openai.rs
   pub struct OpenAIProvider {
       api_key: String,
       base_url: String,
       client: reqwest::Client,
   }
   
   impl AIProvider for OpenAIProvider {
       async fn autocomplete(&self, context: &str) -> Result<String, AIError> {
           // 使用 GPT-3.5-turbo 或 GPT-4o-mini
           // 限制生成长度（20-50 字符）
       }
       
       async fn inline_assist(&self, instruction: &str, text: &str) -> Result<String, AIError> {
           // 使用 GPT-4o 或 GPT-4-turbo
       }
       
       async fn chat_stream(&self, messages: &[Message]) -> Result<Stream, AIError> {
           // 流式响应
       }
   }
   ```

3. **实现流式响应处理**
   - 使用 `tokio_stream` 处理 SSE 流
   - 实时解析 JSON 响应
   - 支持取消操作

**验收标准**：
- ✅ OpenAI 提供商实现完成
- ✅ 流式响应处理实现完成
- ✅ API 调用测试通过
- ✅ 错误处理测试通过

#### 11.2.2 API 密钥管理

**任务**：实现安全的 API 密钥存储

**具体步骤**：

1. **使用系统密钥链存储 API 密钥**
   ```rust
   // src-tauri/src/services/ai_service.rs
   use keyring::Entry;
   
   pub struct APIKeyManager {
       service_name: String,
   }
   
   impl APIKeyManager {
       pub fn save_key(&self, provider: &str, key: &str) -> Result<(), String> {
           let entry = Entry::new(&self.service_name, provider)?;
           entry.set_password(key)?;
           Ok(())
       }
       
       pub fn get_key(&self, provider: &str) -> Result<String, String> {
           let entry = Entry::new(&self.service_name, provider)?;
           entry.get_password().map_err(|e| e.to_string())
       }
   }
   ```

2. **添加 keyring 依赖**
   ```toml
   # src-tauri/Cargo.toml
   [dependencies]
   keyring = "2.0"
   ```

**验收标准**：
- ✅ API 密钥管理实现完成
- ✅ 密钥存储测试通过
- ✅ 安全性验证通过

### 11.3 Tauri 命令注册（Day 6-7）

#### 11.3.1 AI 命令实现

**任务**：实现 Tauri AI 命令

**具体步骤**：

1. **创建 AI 命令模块**
   ```rust
   // src-tauri/src/commands/ai_commands.rs
   use crate::services::ai_service::AIService;
   
   #[tauri::command]
   pub async fn ai_autocomplete(
       context: String,
       position: usize,
       max_length: usize,
   ) -> Result<Option<String>, String> {
       // 调用 AI 服务
   }
   
   #[tauri::command]
   pub async fn ai_inline_assist(
       instruction: String,
       text: String,
       context: String,
   ) -> Result<String, String> {
       // 调用 AI 服务
   }
   
   #[tauri::command]
   pub async fn ai_chat_stream(
       messages: Vec<Message>,
       context: ChatContext,
   ) -> Result<(), String> {
       // 流式响应通过事件发送
   }
   ```

2. **注册命令**
   ```rust
   // src-tauri/src/main.rs
   .invoke_handler(tauri::generate_handler![
       // ... 其他命令
       commands::ai_commands::ai_autocomplete,
       commands::ai_commands::ai_inline_assist,
       commands::ai_commands::ai_chat_stream,
   ])
   ```

**验收标准**：
- ✅ AI 命令实现完成
- ✅ 命令注册完成
- ✅ 前后端通信测试通过

---

## Week 13：层次一 - 自动补全功能

### 目标

实现自动补全功能，包括幽灵文字显示、触发机制、缓存优化等。

### 13.1 自动补全 Hook 实现（Day 1-3）

#### 13.1.1 创建 useAutoComplete Hook

**任务**：实现自动补全 React Hook

**具体步骤**：

1. **创建 Hook 文件**
   ```typescript
   // src/hooks/useAutoComplete.ts
   import { useState, useEffect, useRef, useCallback } from 'react';
   import { invoke } from '@tauri-apps/api/core';
   import { useEditorStore } from '../stores/editorStore';
   
   export interface AutoCompleteState {
       text: string | null;
       position: number | null;
       isVisible: boolean;
   }
   
   export function useAutoComplete(editor: Editor | null) {
       const [state, setState] = useState<AutoCompleteState>({
           text: null,
           position: null,
           isVisible: false,
       });
       
       const triggerTimerRef = useRef<NodeJS.Timeout | null>(null);
       const cancelTokenRef = useRef<string | null>(null);
       const lastContextRef = useRef<string>('');
       
       // 触发自动补全
       const trigger = useCallback(async () => {
           if (!editor) return;
           
           const { from } = editor.state.selection;
           const context = editor.state.doc.textBetween(
               Math.max(0, from - 200),
               from
           );
           
           // 检查上下文是否变化
           if (context === lastContextRef.current) {
               return; // 上下文未变化，不触发
           }
           
           lastContextRef.current = context;
           
           try {
               const result = await invoke<Option<string>>('ai_autocomplete', {
                   context,
                   position: from,
                   maxLength: 50,
               });
               
               if (result) {
                   setState({
                       text: result,
                       position: from,
                       isVisible: true,
                   });
               }
           } catch (error) {
               console.error('自动补全失败:', error);
           }
       }, [editor]);
       
       // 清除自动补全
       const clear = useCallback(() => {
           setState({
               text: null,
               position: null,
               isVisible: false,
           });
           if (triggerTimerRef.current) {
               clearTimeout(triggerTimerRef.current);
               triggerTimerRef.current = null;
           }
       }, []);
       
       // 接受补全
       const accept = useCallback(() => {
           if (!editor || !state.text || !state.position) return;
           
           editor.chain()
               .focus()
               .insertContent(state.text)
               .run();
           
           clear();
       }, [editor, state, clear]);
       
       return {
           state,
           trigger,
           clear,
           accept,
       };
   }
   ```

2. **实现触发机制**
   - 快捷键 Cmd+J（macOS）/ Ctrl+J（Windows）触发
   - 用户输入或移动光标时清除

**验收标准**：
- ✅ useAutoComplete Hook 实现完成
- ✅ 触发机制实现完成
- ✅ 清除机制实现完成
- ✅ 接受机制实现完成
- ✅ 单元测试通过

#### 13.1.2 幽灵文字显示组件（Day 3）⚠️ **高危修复**

**任务**：实现幽灵文字在 TipTap 中的真实渲染

**问题**：只写了 Hook，但没说怎么把文字插到光标后面还不被编辑。用户永远看不到灰色续写文字，或者一打字就乱了。

**具体步骤**：

1. **使用 TipTap Decoration 插件显示幽灵文字**
   ```typescript
   // src/components/Editor/GhostText.tsx
   import { Decoration, DecorationSet } from '@tiptap/pm/view';
   import { Plugin, PluginKey } from '@tiptap/pm/state';
   import { useAutoComplete } from '../../hooks/useAutoComplete';
   
   interface GhostTextProps {
       editor: Editor | null;
   }
   
   // 创建 Decoration 插件
   const ghostTextPluginKey = new PluginKey('ghostText');
   
   export const createGhostTextPlugin = (getGhostText: () => { text: string; position: number } | null) => {
       return new Plugin({
           key: ghostTextPluginKey,
           state: {
               init() {
                   return DecorationSet.empty;
               },
               apply(tr, set) {
                   const ghostText = getGhostText();
                   if (!ghostText) {
                       return DecorationSet.empty;
                   }
                   
                   const { text, position } = ghostText;
                   const from = position;
                   const to = position + text.length;
                   
                   // 创建 inline decoration
                   const decoration = Decoration.inline(from, to, {
                       class: 'ghost-text',
                       style: 'color: #999; opacity: 0.6; pointer-events: none;',
                   });
                   
                   return DecorationSet.create(tr.doc, [decoration]);
               },
           },
           props: {
               decorations(state) {
                   return this.getState(state);
               },
           },
       });
   };
   
   // 在 TipTapEditor 中使用
   // src/components/Editor/TipTapEditor.tsx
   import { createGhostTextPlugin } from './GhostText';
   
   const editor = useEditor({
       extensions: [
           StarterKit,
           // ... 其他扩展
       ],
       onUpdate: ({ editor }) => {
           // 清除幽灵文字
           clearGhostText();
       },
       editorProps: {
           // 用户输入时清除幽灵文字
           handleKeyDown: (view, event) => {
               if (event.key !== 'Tab' && event.key !== 'Escape') {
                   clearGhostText();
               }
           },
       },
   });
   
   // 添加插件
   useEffect(() => {
       if (editor) {
           const plugin = createGhostTextPlugin(() => {
               const { state } = useAutoComplete(editor);
               if (state.isVisible && state.text && state.position !== null) {
                   return { text: state.text, position: state.position };
               }
               return null;
           });
           editor.view.dispatch(
               editor.state.tr.setMeta('addToHistory', false)
           );
       }
   }, [editor]);
   ```

2. **添加 CSS 样式**
   ```css
   /* src/index.css */
   .ghost-text {
       color: #999 !important;
       opacity: 0.6 !important;
       pointer-events: none !important;
       user-select: none !important;
   }
   ```

3. **处理 Tab 键接受补全**
   ```typescript
   // 在 TipTapEditor 中
   editor.setOptions({
       editorProps: {
           handleKeyDown: (view, event) => {
               if (event.key === 'Tab' && ghostTextState.isVisible) {
                   event.preventDefault();
                   acceptGhostText();
                   return true;
               }
               if (event.key === 'Escape' && ghostTextState.isVisible) {
                   event.preventDefault();
                   clearGhostText();
                   return true;
               }
               return false;
           },
       },
   });
   ```

**验收标准**：
- ✅ 幽灵文字使用 TipTap Decoration 正确渲染
- ✅ 幽灵文字显示在光标后方，半透明灰色
- ✅ 用户输入时自动清除幽灵文字
- ✅ Tab 键接受补全，Escape 键清除
- ✅ 不影响编辑器正常编辑功能
- ✅ 样式正确（半透明、灰色、不可选中）

#### 13.1.3 缓存和性能优化

**任务**：实现请求缓存和性能优化

**具体步骤**：

1. **实现 LRU 缓存**
   ```rust
   // src-tauri/src/services/ai_service.rs
   use lru::LruCache;
   
   pub struct AutoCompleteCache {
       cache: LruCache<String, String>,
       max_size: usize,
   }
   
   impl AutoCompleteCache {
       pub fn get(&mut self, key: &str) -> Option<String> {
           self.cache.get(key).cloned()
       }
       
       pub fn put(&mut self, key: String, value: String) {
           self.cache.put(key, value);
       }
   }
   ```

2. **实现请求去重**
   - 相同上下文的请求复用结果
   - 使用请求 ID 去重

3. **实现请求取消**
   - 用户继续输入时自动取消之前的请求
   - 使用 CancellationToken 实现

**验收标准**：
- ✅ 缓存机制实现完成
- ✅ 请求去重实现完成
- ✅ 请求取消实现完成
- ✅ 性能测试通过

---

## Week 14：层次二 - Inline Assist 功能

### 目标

实现 Inline Assist 功能，包括 Cmd+K 快捷键、输入框、Diff 视图等。

### 14.1 Inline Assist Hook 实现（Day 1-2）

#### 14.1.1 创建 useInlineAssist Hook

**任务**：实现 Inline Assist React Hook

**具体步骤**：

1. **创建 Hook 文件**
   ```typescript
   // src/hooks/useInlineAssist.ts
   import { useState, useCallback } from 'react';
   import { invoke } from '@tauri-apps/api/core';
   import { listen } from '@tauri-apps/api/event';
   
   export interface InlineAssistState {
       isVisible: boolean;
       instruction: string;
       selectedText: string;
       diff: string | null;
       isLoading: boolean;
   }
   
   export function useInlineAssist(editor: Editor | null) {
       const [state, setState] = useState<InlineAssistState>({
           isVisible: false,
           instruction: '',
           selectedText: '',
           diff: null,
           isLoading: false,
       });
       
       // 打开 Inline Assist
       const open = useCallback((selectedText: string) => {
           setState({
               isVisible: true,
               instruction: '',
               selectedText,
               diff: null,
               isLoading: false,
           });
       }, []);
       
       // 关闭 Inline Assist
       const close = useCallback(() => {
           setState({
               isVisible: false,
               instruction: '',
               selectedText: '',
               diff: null,
               isLoading: false,
           });
       }, []);
       
       // 提交指令
       const submit = useCallback(async () => {
           if (!editor || !state.instruction) return;
           
           setState(prev => ({ ...prev, isLoading: true }));
           
           try {
               const result = await invoke<string>('ai_inline_assist', {
                   instruction: state.instruction,
                   text: state.selectedText,
                   context: editor.state.doc.textContent(),
               });
               
               setState(prev => ({
                   ...prev,
                   diff: result,
                   isLoading: false,
               }));
           } catch (error) {
               console.error('Inline Assist 失败:', error);
               setState(prev => ({ ...prev, isLoading: false }));
           }
       }, [editor, state.instruction, state.selectedText]);
       
       return {
           state,
           open,
           close,
           submit,
       };
   }
   ```

**验收标准**：
- ✅ useInlineAssist Hook 实现完成
- ✅ 打开/关闭机制实现完成
- ✅ 提交机制实现完成
- ✅ 单元测试通过

#### 14.1.2 Inline Assist 输入框组件

**任务**：实现 Inline Assist 输入框组件

**具体步骤**：

1. **创建 InlineAssistInput 组件**
   ```typescript
   // src/components/Editor/InlineAssistInput.tsx
   import React, { useEffect, useRef } from 'react';
   import { useInlineAssist } from '../../hooks/useInlineAssist';
   
   interface InlineAssistInputProps {
       editor: Editor | null;
       position: { x: number; y: number };
   }
   
   export const InlineAssistInput: React.FC<InlineAssistInputProps> = ({
       editor,
       position,
   }) => {
       const { state, close, submit } = useInlineAssist(editor);
       const inputRef = useRef<HTMLInputElement>(null);
       
       useEffect(() => {
           if (state.isVisible && inputRef.current) {
               inputRef.current.focus();
           }
       }, [state.isVisible]);
       
       if (!state.isVisible) {
           return null;
       }
       
       return (
           <div
               className="fixed bg-white dark:bg-gray-800 border border-gray-300 dark:border-gray-600 rounded-lg shadow-lg p-2 z-50"
               style={{
                   left: `${position.x}px`,
                   top: `${position.y}px`,
               }}
           >
               <input
                   ref={inputRef}
                   type="text"
                   value={state.instruction}
                   onChange={(e) => setState(prev => ({ ...prev, instruction: e.target.value }))}
                   onKeyDown={(e) => {
                       if (e.key === 'Enter') {
                           submit();
                       } else if (e.key === 'Escape') {
                           close();
                       }
                   }}
                   placeholder="输入指令，如：改得更正式"
                   className="w-64 px-2 py-1 border border-gray-300 dark:border-gray-600 rounded"
               />
               {state.isLoading && (
                   <div className="text-sm text-gray-500 mt-1">处理中...</div>
               )}
           </div>
       );
   };
   ```

**验收标准**：
- ✅ InlineAssistInput 组件实现完成
- ✅ 输入框显示正常
- ✅ 快捷键支持（Enter 提交，Escape 关闭）
- ✅ 样式正确

#### 14.1.3 Diff 视图组件

**任务**：实现 Diff 视图组件

**具体步骤**：

1. **创建 DiffView 组件**
   ```typescript
   // src/components/Editor/DiffView.tsx
   import React from 'react';
   
   interface DiffViewProps {
       original: string;
       updated: string;
       onAccept: () => void;
       onCancel: () => void;
   }
   
   export const DiffView: React.FC<DiffViewProps> = ({
       original,
       updated,
       onAccept,
       onCancel,
   }) => {
       return (
           <div className="border border-gray-300 dark:border-gray-600 rounded-lg p-4">
               <div className="mb-2">
                   <div className="text-sm font-semibold mb-1">原始文本：</div>
                   <div className="bg-red-50 dark:bg-red-900/20 p-2 rounded">
                       {original}
                   </div>
               </div>
               <div className="mb-2">
                   <div className="text-sm font-semibold mb-1">修改后：</div>
                   <div className="bg-green-50 dark:bg-green-900/20 p-2 rounded">
                       {updated}
                   </div>
               </div>
               <div className="flex gap-2">
                   <button
                       onClick={onAccept}
                       className="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700"
                   >
                       应用
                   </button>
                   <button
                       onClick={onCancel}
                       className="px-4 py-2 bg-gray-300 dark:bg-gray-600 rounded hover:bg-gray-400"
                   >
                       取消
                   </button>
               </div>
           </div>
       );
   };
   ```

**验收标准**：
- ✅ DiffView 组件实现完成
- ✅ Diff 显示正确（红色删除，绿色添加）
- ✅ 应用/取消按钮功能正常
- ✅ 样式正确

#### 14.1.4 Cmd+K 快捷键集成

**任务**：集成 Cmd+K 快捷键

**具体步骤**：

1. **在 EditorPanel 中添加快捷键监听**
   ```typescript
   // src/components/Editor/EditorPanel.tsx
   useEffect(() => {
       const handleKeyDown = (e: KeyboardEvent) => {
           const isMac = navigator.platform.toUpperCase().indexOf('MAC') >= 0;
           const modifier = isMac ? e.metaKey : e.ctrlKey;
           
           if (modifier && e.key === 'k' && !e.shiftKey && !e.altKey) {
               e.preventDefault();
               const selectedText = editor?.state.doc.textBetween(
                   editor.state.selection.from,
                   editor.state.selection.to
               ) || '';
               inlineAssist.open(selectedText);
           }
       };
       
       window.addEventListener('keydown', handleKeyDown);
       return () => window.removeEventListener('keydown', handleKeyDown);
   }, [editor, inlineAssist]);
   ```

**验收标准**：
- ✅ Cmd+K 快捷键集成完成
- ✅ 快捷键触发正常
- ✅ 选中文本时传递选中内容
- ✅ 未选中时传递光标位置上下文

---

## Week 15：层次三 - 聊天窗口功能

### 目标

实现完整的聊天窗口功能，包括多标签、模型切换、记忆库、文件引用等。

### 15.1 聊天窗口基础功能（Day 1-3）

#### 15.1.1 聊天状态管理

**任务**：实现聊天状态管理

**具体步骤**：

1. **创建聊天 Store**
   ```typescript
   // src/stores/chatStore.ts
   import { create } from 'zustand';
   
   export interface ChatMessage {
       id: string;
       role: 'user' | 'assistant';
       content: string;
       timestamp: number;
       references?: string[]; // 引用的文件路径
   }
   
   export interface ChatTab {
       id: string;
       title: string;
       messages: ChatMessage[];
       model: string;
       isStreaming: boolean;
   }
   
   interface ChatStore {
       tabs: ChatTab[];
       activeTabId: string | null;
       addTab: () => void;
       removeTab: (id: string) => void;
       setActiveTab: (id: string) => void;
       addMessage: (tabId: string, message: ChatMessage) => void;
       setModel: (tabId: string, model: string) => void;
   }
   
   export const useChatStore = create<ChatStore>((set) => ({
       tabs: [],
       activeTabId: null,
       addTab: () => {
           const newTab: ChatTab = {
               id: `chat-${Date.now()}`,
               title: `聊天 ${Date.now()}`,
               messages: [],
               model: 'gpt-4o',
               isStreaming: false,
           };
           set((state) => ({
               tabs: [...state.tabs, newTab],
               activeTabId: newTab.id,
           }));
       },
       removeTab: (id: string) => {
           set((state) => ({
               tabs: state.tabs.filter((t) => t.id !== id),
               activeTabId: state.activeTabId === id 
                   ? state.tabs.find((t) => t.id !== id)?.id || null
                   : state.activeTabId,
           }));
       },
       setActiveTab: (id: string) => {
           set({ activeTabId: id });
       },
       addMessage: (tabId: string, message: ChatMessage) => {
           set((state) => ({
               tabs: state.tabs.map((tab) =>
                   tab.id === tabId
                       ? { ...tab, messages: [...tab.messages, message] }
                       : tab
               ),
           }));
       },
       setModel: (tabId: string, model: string) => {
           set((state) => ({
               tabs: state.tabs.map((tab) =>
                   tab.id === tabId ? { ...tab, model } : tab
               ),
           }));
       },
   }));
   ```

**验收标准**：
- ✅ 聊天 Store 实现完成
- ✅ 多标签管理实现完成
- ✅ 消息管理实现完成
- ✅ 模型切换实现完成
- ✅ 单元测试通过

#### 15.1.2 聊天窗口组件

**任务**：实现聊天窗口组件

**具体步骤**：

1. **创建 ChatPanel 组件**
   ```typescript
   // src/components/Chat/ChatPanel.tsx
   import React from 'react';
   import { useChatStore } from '../../stores/chatStore';
   import { ChatTabs } from './ChatTabs';
   import { ChatMessages } from './ChatMessages';
   import { ChatInput } from './ChatInput';
   
   export const ChatPanel: React.FC = () => {
       const { tabs, activeTabId, addTab, removeTab, setActiveTab } = useChatStore();
       const activeTab = tabs.find((t) => t.id === activeTabId);
       
       return (
           <div className="h-full flex flex-col bg-white dark:bg-gray-800">
               <ChatTabs
                   tabs={tabs}
                   activeTabId={activeTabId}
                   onAddTab={addTab}
                   onRemoveTab={removeTab}
                   onSelectTab={setActiveTab}
               />
               <ChatMessages messages={activeTab?.messages || []} />
               <ChatInput tabId={activeTabId} />
           </div>
       );
   };
   ```

2. **创建子组件**
   - ChatTabs：标签栏
   - ChatMessages：消息列表
   - ChatInput：输入框

**验收标准**：
- ✅ ChatPanel 组件实现完成
- ✅ 子组件实现完成
- ✅ 布局正确
- ✅ 样式正确

#### 15.1.3 流式响应处理（Day 2）⚠️ **高危修复**

**任务**：实现流式响应的前端实时渲染

**问题**：后端只写了 `emit_all("ai-chat-stream", ...)`，但前端根本没写如何把 chunk 拼成一条完整消息。用户发完问题后，聊天窗口永远卡在"正在思考…"，实际在疯狂收到 chunk 但不显示。

**具体步骤**：

1. **实现流式响应监听和消息拼接**
   ```typescript
   // src/services/chatService.ts
   import { listen } from '@tauri-apps/api/event';
   import { invoke } from '@tauri-apps/api/core';
   import { useChatStore } from '../stores/chatStore';
   
   export const chatService = {
       async sendMessage(tabId: string, content: string, context: ChatContext) {
           const { addMessage, updateMessageContent, setStreaming } = useChatStore.getState();
           
           // 添加用户消息
           addMessage(tabId, {
               id: `msg-user-${Date.now()}`,
               role: 'user',
               content,
               timestamp: Date.now(),
           });
           
           // 创建空的助手消息，用于流式更新
           const assistantMessageId = `msg-assistant-${Date.now()}`;
           addMessage(tabId, {
               id: assistantMessageId,
               role: 'assistant',
               content: '', // 初始为空
               timestamp: Date.now(),
           });
           
           // 开始流式响应
           setStreaming(tabId, true);
           
           // 累积接收到的内容
           let accumulatedContent = '';
           
           // 监听流式响应事件
           const unlisten = await listen<{
               tab_id: string;
               chunk: string;
               done: boolean;
           }>('ai-chat-stream', (event) => {
               const { tab_id, chunk, done } = event.payload;
               
               if (tab_id === tabId) {
                   if (done) {
                       // 流式响应完成
                       setStreaming(tabId, false);
                       unlisten();
                       
                       // 确保最后的内容被保存
                       if (accumulatedContent) {
                           updateMessageContent(tabId, assistantMessageId, accumulatedContent);
                       }
                   } else {
                       // 累积 chunk
                       accumulatedContent += chunk;
                       
                       // 实时更新消息内容（节流，避免过于频繁的更新）
                       updateMessageContent(tabId, assistantMessageId, accumulatedContent);
                   }
               }
           });
           
           try {
               // 调用后端命令
               await invoke('ai_chat_stream', {
                   tabId,
                   messages: context.messages,
                   context,
               });
           } catch (error) {
               console.error('发送消息失败:', error);
               setStreaming(tabId, false);
               unlisten();
               
               // 更新消息显示错误
               updateMessageContent(tabId, assistantMessageId, `错误: ${error}`);
           }
       },
   };
   ```

2. **更新 ChatStore 添加消息更新方法**
   ```typescript
   // src/stores/chatStore.ts
   interface ChatStore {
       // ... 其他方法
       updateMessageContent: (tabId: string, messageId: string, content: string) => void;
   }
   
   export const useChatStore = create<ChatStore>((set) => ({
       // ... 其他状态
       updateMessageContent: (tabId: string, messageId: string, content: string) => {
           set((state) => ({
               tabs: state.tabs.map((tab) =>
                   tab.id === tabId
                       ? {
                           ...tab,
                           messages: tab.messages.map((msg) =>
                               msg.id === messageId
                                   ? { ...msg, content }
                                   : msg
                           ),
                       }
                       : tab
               ),
           }));
       },
   }));
   ```

3. **在 ChatMessages 组件中实时显示**
   ```typescript
   // src/components/Chat/ChatMessages.tsx
   export const ChatMessages: React.FC<{ messages: ChatMessage[] }> = ({ messages }) => {
       return (
           <div className="flex-1 overflow-y-auto p-4 space-y-4">
               {messages.map((message) => (
                   <div
                       key={message.id}
                       className={`flex ${message.role === 'user' ? 'justify-end' : 'justify-start'}`}
                   >
                       <div
                           className={`max-w-3xl rounded-lg p-3 ${
                               message.role === 'user'
                                   ? 'bg-blue-600 text-white'
                                   : 'bg-gray-100 dark:bg-gray-700 text-gray-900 dark:text-gray-100'
                           }`}
                       >
                           {/* 实时显示内容，支持 Markdown */}
                           <div className="prose dark:prose-invert max-w-none">
                               {message.content || (
                                   <span className="text-gray-400">正在思考...</span>
                               )}
                           </div>
                       </div>
                   </div>
               ))}
           </div>
       );
   };
   ```

4. **添加消息输入 loading 动画**（中危修复）
   ```typescript
   // src/components/Chat/ChatMessages.tsx
   {message.role === 'assistant' && !message.content && (
       <div className="flex items-center gap-2 text-gray-400">
           <div className="flex gap-1">
               <div className="w-2 h-2 bg-gray-400 rounded-full animate-bounce" style={{ animationDelay: '0ms' }} />
               <div className="w-2 h-2 bg-gray-400 rounded-full animate-bounce" style={{ animationDelay: '150ms' }} />
               <div className="w-2 h-2 bg-gray-400 rounded-full animate-bounce" style={{ animationDelay: '300ms' }} />
           </div>
           <span className="text-sm">正在思考...</span>
       </div>
   )}
   ```

**验收标准**：
- ✅ 流式响应监听实现完成
- ✅ chunk 正确累积成完整消息
- ✅ 消息实时更新显示正常
- ✅ loading 动画显示正常
- ✅ 错误处理完善
- ✅ 性能测试通过（节流更新，避免卡顿）

### 15.2 记忆库功能（Day 4-5）⚠️ **中危修复**

#### 15.2.1 记忆库服务实现

**任务**：实现记忆库后端服务，支持手动标记和 AI 辅助识别

**问题**：记忆库完全是"纯手动标记"，但计划里写了"自动识别人物/事件"。这在中文长篇里准确率 < 50%，用户会失望。

**解决方案**：采用"手动标记 + AI 辅助建议"的混合模式，不强制自动识别。

**具体步骤**：

1. **创建记忆库服务**
   ```rust
   // src-tauri/src/services/memory_service.rs
   use rusqlite::{Connection, Result};
   use serde::{Deserialize, Serialize};
   
   #[derive(Debug, Clone, Serialize, Deserialize)]
   pub struct Memory {
       pub id: String,
       pub document_path: String,
       pub entity_type: String, // "character", "event", "timeline", "location", "custom"
       pub entity_name: String,
       pub content: String,
       pub metadata: serde_json::Value,
       pub source: String, // "manual" 或 "ai_suggested"
       pub confidence: f64, // AI 建议的置信度（0.0-1.0）
   }
   
   pub struct MemoryService {
       db: Connection,
   }
   
   impl MemoryService {
       pub fn new(db_path: &str) -> Result<Self> {
           let db = Connection::open(db_path)?;
           db.execute(
               "CREATE TABLE IF NOT EXISTS memories (
                   id TEXT PRIMARY KEY,
                   document_path TEXT NOT NULL,
                   entity_type TEXT NOT NULL,
                   entity_name TEXT NOT NULL,
                   content TEXT NOT NULL,
                   metadata TEXT NOT NULL,
                   source TEXT NOT NULL DEFAULT 'manual',
                   confidence REAL DEFAULT 1.0
               )",
               [],
           )?;
           Ok(Self { db })
       }
       
       // 手动添加记忆
       pub fn add_memory(&self, memory: Memory) -> Result<()> {
           self.db.execute(
               "INSERT OR REPLACE INTO memories (id, document_path, entity_type, entity_name, content, metadata, source, confidence)
                VALUES (?1, ?2, ?3, ?4, ?5, ?6, ?7, ?8)",
               &[
                   &memory.id,
                   &memory.document_path,
                   &memory.entity_type,
                   &memory.entity_name,
                   &memory.content,
                   &serde_json::to_string(&memory.metadata).unwrap(),
                   &memory.source,
                   &memory.confidence.to_string(),
               ],
           )?;
           Ok(())
       }
       
       // AI 辅助识别（可选，不强制）
       pub async fn suggest_memories(&self, document_path: &str, content: &str) -> Result<Vec<Memory>> {
           // 调用 AI 服务识别实体（可选功能）
           // 返回建议列表，用户可以选择接受或拒绝
           // 默认不自动添加，只作为建议
           Ok(vec![])
       }
       
       pub fn get_memories(&self, document_path: &str) -> Result<Vec<Memory>> {
           // 获取文档的所有记忆
       }
       
       pub fn search_memories(&self, query: &str) -> Result<Vec<Memory>> {
           // 搜索记忆
       }
   }
   ```

2. **前端记忆库 UI 支持手动添加和 AI 建议**
   ```typescript
   // src/components/Chat/MemoryTab.tsx
   export const MemoryTab: React.FC = () => {
       const [memories, setMemories] = React.useState<Memory[]>([]);
       const [showAddDialog, setShowAddDialog] = React.useState(false);
       const [aiSuggestions, setAiSuggestions] = React.useState<Memory[]>([]);
       
       // 手动添加记忆
       const handleAddMemory = async (memory: Omit<Memory, 'id'>) => {
           await invoke('add_memory', { memory: { ...memory, id: uuid(), source: 'manual', confidence: 1.0 } });
           loadMemories();
       };
       
       // 请求 AI 建议（可选）
       const handleRequestSuggestions = async () => {
           const suggestions = await invoke<Memory[]>('suggest_memories', {
               documentPath: currentDocumentPath,
           });
           setAiSuggestions(suggestions);
       };
       
       return (
           <div className="p-4">
               <div className="flex justify-between items-center mb-4">
                   <h3 className="text-lg font-semibold">记忆库</h3>
                   <div className="flex gap-2">
                       <button
                           onClick={handleRequestSuggestions}
                           className="px-3 py-1 text-sm bg-blue-600 text-white rounded hover:bg-blue-700"
                       >
                           AI 建议
                       </button>
                       <button
                           onClick={() => setShowAddDialog(true)}
                           className="px-3 py-1 text-sm bg-green-600 text-white rounded hover:bg-green-700"
                       >
                           手动添加
                       </button>
                   </div>
               </div>
               
               {/* AI 建议区域（可选） */}
               {aiSuggestions.length > 0 && (
                   <div className="mb-4 p-3 bg-yellow-50 dark:bg-yellow-900/20 rounded border border-yellow-200 dark:border-yellow-800">
                       <div className="text-sm font-semibold mb-2">AI 建议（请选择接受或忽略）：</div>
                       {aiSuggestions.map((suggestion) => (
                           <div key={suggestion.id} className="flex items-center justify-between p-2 mb-2 bg-white dark:bg-gray-800 rounded">
                               <div>
                                   <div className="font-semibold">{suggestion.entity_name}</div>
                                   <div className="text-xs text-gray-500">置信度: {(suggestion.confidence * 100).toFixed(0)}%</div>
                               </div>
                               <div className="flex gap-2">
                                   <button onClick={() => handleAddMemory(suggestion)} className="text-sm text-green-600">接受</button>
                                   <button onClick={() => setAiSuggestions(prev => prev.filter(m => m.id !== suggestion.id))} className="text-sm text-gray-400">忽略</button>
                               </div>
                           </div>
                       ))}
                   </div>
               )}
               
               {/* 记忆列表 */}
               <div className="space-y-2">
                   {memories.map((memory) => (
                       <div key={memory.id} className="border rounded p-2">
                           <div className="font-semibold">{memory.entity_name}</div>
                           <div className="text-sm text-gray-500">{memory.entity_type}</div>
                           <div className="text-sm">{memory.content}</div>
                       </div>
                   ))}
               </div>
           </div>
       );
   };
   ```

**验收标准**：
- ✅ 记忆库服务实现完成
- ✅ 支持手动添加记忆
- ✅ AI 建议功能可选（不强制自动识别）
- ✅ 数据库操作正常
- ✅ 搜索功能实现完成
- ✅ 单元测试通过

#### 15.2.2 记忆库 UI 组件

**任务**：实现记忆库 UI 组件

**具体步骤**：

1. **创建 MemoryTab 组件**
   ```typescript
   // src/components/Chat/MemoryTab.tsx
   import React from 'react';
   import { invoke } from '@tauri-apps/api/core';
   
   interface Memory {
       id: string;
       document_path: string;
       entity_type: string;
       entity_name: string;
       content: string;
   }
   
   export const MemoryTab: React.FC = () => {
       const [memories, setMemories] = React.useState<Memory[]>([]);
       
       React.useEffect(() => {
           loadMemories();
       }, []);
       
       const loadMemories = async () => {
           try {
               const result = await invoke<Memory[]>('get_memories', {
                   documentPath: currentDocumentPath,
               });
               setMemories(result);
           } catch (error) {
               console.error('加载记忆失败:', error);
           }
       };
       
       return (
           <div className="p-4">
               <h3 className="text-lg font-semibold mb-4">记忆库</h3>
               <div className="space-y-2">
                   {memories.map((memory) => (
                       <div key={memory.id} className="border rounded p-2">
                           <div className="font-semibold">{memory.entity_name}</div>
                           <div className="text-sm text-gray-500">{memory.entity_type}</div>
                           <div className="text-sm">{memory.content}</div>
                       </div>
                   ))}
               </div>
           </div>
       );
   };
   ```

**验收标准**：
- ✅ MemoryTab 组件实现完成
- ✅ 记忆显示正常
- ✅ 搜索功能实现完成
- ✅ 样式正确

---

## Week 16：SQLite 全文索引和优化

### 目标

实现 SQLite 全文索引功能，优化搜索性能，完善 AI 功能。

### 16.1 SQLite 全文索引实现（Day 1-3）

#### 16.1.1 FTS5 全文索引服务

**任务**：实现 SQLite FTS5 全文索引

**具体步骤**：

1. **添加依赖**
   ```toml
   # src-tauri/Cargo.toml
   [dependencies]
   rusqlite = { version = "0.31", features = ["bundled", "fts5"] }
   ```

2. **实现全文索引服务**
   ```rust
   // src-tauri/src/services/search_service.rs
   use rusqlite::{Connection, Result};
   
   pub struct SearchService {
       db: Connection,
   }
   
   impl SearchService {
       pub fn new(db_path: &str) -> Result<Self> {
           let db = Connection::open(db_path)?;
           
           // 创建 FTS5 虚拟表
           db.execute(
               "CREATE VIRTUAL TABLE IF NOT EXISTS documents_fts USING fts5(
                   path,
                   content,
                   title
               )",
               [],
           )?;
           
           Ok(Self { db })
       }
       
       pub fn index_document(&self, path: &str, content: &str, title: &str) -> Result<()> {
           // 索引文档
           self.db.execute(
               "INSERT OR REPLACE INTO documents_fts (path, content, title) VALUES (?1, ?2, ?3)",
               &[path, content, title],
           )?;
           Ok(())
       }
       
       pub fn search(&self, query: &str) -> Result<Vec<SearchResult>> {
           // 全文搜索
           let mut stmt = self.db.prepare(
               "SELECT path, title, snippet(documents_fts, 2, '<mark>', '</mark>', '...', 32) as snippet
                FROM documents_fts
                WHERE documents_fts MATCH ?1
                ORDER BY rank
                LIMIT 50"
           )?;
           
           let results = stmt.query_map(&[query], |row| {
               Ok(SearchResult {
                   path: row.get(0)?,
                   title: row.get(1)?,
                   snippet: row.get(2)?,
               })
           })?;
           
           results.collect()
       }
   }
   ```

**验收标准**：
- ✅ FTS5 全文索引实现完成
- ✅ 文档索引功能正常
- ✅ 搜索功能实现完成
- ✅ 性能测试通过（毫秒级搜索）

#### 16.1.2 增量索引更新

**任务**：实现增量索引更新

**具体步骤**：

1. **监听文件变化**
   - 使用文件监听服务监听文件变化
   - 文件修改时自动更新索引
   - 文件删除时自动删除索引

2. **批量索引**
   - 工作区打开时批量索引所有文档
   - 使用后台任务，不阻塞 UI

**验收标准**：
- ✅ 增量索引更新实现完成
- ✅ 文件监听集成完成
- ✅ 批量索引实现完成
- ✅ 性能测试通过

### 16.2 AI 功能优化和测试（Day 4-5）

#### 16.2.1 性能优化

**任务**：优化 AI 功能性能

**具体步骤**：

1. **请求缓存优化**
   - 优化 LRU 缓存大小
   - 实现更智能的缓存策略

2. **请求去重优化**
   - 优化去重算法
   - 减少重复请求

3. **流式响应优化**
   - 优化流式响应处理
   - 减少内存占用

**验收标准**：
- ✅ 性能优化完成
- ✅ 性能测试通过
- ✅ 内存占用测试通过

#### 16.2.2 测试和文档

**任务**：完善测试和文档

**具体步骤**：

1. **单元测试**
   - AI 服务单元测试
   - 自动补全单元测试
   - Inline Assist 单元测试
   - 聊天功能单元测试

2. **集成测试**
   - AI 功能集成测试
   - 全文搜索集成测试

3. **文档更新**
   - API 文档更新
   - 用户手册更新
   - 开发文档更新

**验收标准**：
- ✅ 测试覆盖率达到 80% 以上
- ✅ 所有测试通过
- ✅ 文档更新完成

---

## 开发时间表

| 周次 | 任务 | 工作日 | 优先级 |
|------|------|--------|--------|
| Week 11 | AI 服务基础架构 | 5 天 | P0 |
| Week 12 | OpenAI 提供商实现 | 5 天 | P0 |
| Week 13 | 自动补全功能 | 5 天 | P0 |
| Week 14 | Inline Assist 功能 | 5 天 | P0 |
| Week 15 | 聊天窗口功能 | 5 天 | P0 |
| Week 16 | SQLite 全文索引和优化 | 5 天 | P0 |

**总计**：30 个工作日（6 周）

---

## 依赖项检查清单

### 前端依赖
- [ ] `@tauri-apps/api` - Tauri API（已安装）
- [ ] `zustand` - 状态管理（已安装）
- [ ] `@heroicons/react` - 图标库（已安装）

### 后端依赖
- [ ] `reqwest` - HTTP 客户端
- [ ] `tokio-stream` - 流式处理
- [ ] `keyring` - 密钥管理
- [ ] `rusqlite` - SQLite 数据库（FTS5 支持）
- [ ] `lru` - LRU 缓存

### 权限配置
- [ ] 网络请求权限（AI API 调用）
- [ ] 文件系统权限（已配置）
- [ ] 事件监听权限（流式响应）

---

## 测试计划

### 单元测试
- [ ] AI 服务测试
- [ ] 自动补全测试
- [ ] Inline Assist 测试
- [ ] 聊天功能测试
- [ ] 全文搜索测试

### 集成测试
- [ ] AI 功能集成测试
- [ ] 全文搜索集成测试
- [ ] 性能测试

### 手动测试
- [ ] 自动补全功能测试
- [ ] Inline Assist 功能测试
- [ ] 聊天功能测试
- [ ] 全文搜索测试
- [ ] 错误场景测试

---

## 文档更新

### 需要更新的文档
- [ ] 开发进度文档
- [ ] API 文档
- [ ] 用户手册（AI 功能使用说明）
- [ ] 开发文档（AI 功能实现细节）

---

**计划版本**：v3.0  
**创建日期**：2025年  
**下次更新**：根据开发进度调整

