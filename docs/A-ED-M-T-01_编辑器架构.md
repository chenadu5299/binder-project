# 编辑器架构

## 文档头

- 结构编码：`ED-M-T-01`
- 文档属性：`主结构`
- 主责模块：`ED`
- 文档职责：`编辑器架构 / 模型、架构与机制主控`
- 上游约束：`CORE-C-D-04`, `SYS-C-T-01`, `WS-M-T-01`
- 直接承接：无
- 接口耦合：`WS-M-T-01`, `SYS-I-P-01`, `ENG-X-T-01`
- 汇聚影响：`CORE-C-R-01`
- 扩散检查：`ED-M-T-02`, `ED-M-T-03`, `ED-M-T-04`, `ED-M-T-05`, `ED-M-T-06`
- 使用边界：`定义技术模型、实现约束与关键机制，不承担产品边界裁定与排期管理`
- 变更要求：`修改本文后，必须复核：上游约束、直接承接、接口耦合、汇聚影响、扩散检查文档`

---
## 一、文档定位

本文是 30_capabilities/02_编辑器系统 的主干入口文档。  
目标：统一编辑器主干架构，并明确与 block、分页、docx、资源与 AI 子系统的引用关系。

---

## 二、编辑器主干目标（MVP）

1. 提供稳定的多格式文档编辑主链（md/txt/html/docx）。  
2. 保证打开、编辑、保存、回读的一致性。  
3. 为 AI 对话编辑提供可定位、可执行、可观测的编辑基础。  

---

## 三、主干分层

1. 交互层  
EditorPanel、工具栏、只读提示、预览/编辑切换。  

2. 编辑核心层  
TipTap 文档模型、节点系统、输入与事务。  

3. 文件适配层  
按文件类型分流（文本直编、docx 转换、预览模式）。  

4. 持久化与缓存层  
保存写盘、缓存同步、外部修改感知。  

---

## 四、主干与子方案引用关系（核心补齐）

| 子方案 | 角色 | 对 `A-ED-M-T-01_编辑器架构.md` 的输入 | 对 `A-ED-M-T-01_编辑器架构.md` 的输出 |
|---|---|---|---|
| `A-ED-M-T-02_block模型.md` | 定位与区间语义基础 | blockId、区间映射规则 | 为编辑执行和 Diff 定位提供统一坐标语义 |
| `A-ED-M-T-03_分页系统.md` | 视图呈现增强层 | 文档结构与布局信息 | 不改变主干内容真源，仅改变分页显示 |
| `A-ED-M-T-04_docx能力.md` | 文件适配专项 | docx 转换与样式策略 | 提供 docx 可编辑链路并回写主干保存链 |
| `A-WS-M-T-01_workspace架构.md` | 外部协同层 | 文件缓存、打开策略、状态同步 | 与编辑器共同保证打开/保存/回读一致 |
| ``A-DE-M-D-01_对话编辑统一方案.md`/`A-DE-M-T-01_diff系统规则.md`/`A-DE-M-T-02_baseline状态协作.md`/`A-DE-M-P-01_对话编辑提示词.md`` | AI 对话编辑协同层 | 目标文档实例、区间定位能力 | 使用编辑器主干能力执行可确认修改 |

---

## 五、关键主链

## 5.1 打开链

文件识别 -> 策略分流 -> 内容加载 -> 编辑器实例化。  

## 5.2 编辑链

用户输入/命令 -> 编辑事务 -> 状态更新 -> 视图同步。  

## 5.3 保存链

内容序列化 -> 写盘 -> 缓存同步 -> 回读校验。  

## 5.4 外部修改链

mtime 检测 -> 冲突处理 -> 内容重载或保留 -> 状态修正。  

---

## 六、跨系统边界约束

1. 编辑器是“内容执行层”，不是 AI 决策层。  
2. 展示增强（分页/装饰）不得替代逻辑内容真源。  
3. 文档写盘动作必须经过统一保存链。  
4. AI 改写必须经过编辑器区间执行能力，不绕过事务。  

---

## 七、MVP 验收口径

1. 文档主干链（打开/编辑/保存/回读）稳定。  
2. 与 `A-ED-M-T-02_block模型.md`/`A-ED-M-T-03_分页系统.md`/`A-ED-M-T-04_docx能力.md` 的职责边界明确，不互相越权。  
3. 与 `A-WS-M-T-01_workspace架构.md`、`A-DE-M-D-01_对话编辑统一方案.md`~`A-DE-M-P-01_对话编辑提示词.md` 的协作链路可复现。  
4. 同一内容在不同视图模式下逻辑结果一致。  

---

## 八、关联文档

1. `R-ED-M-R-21_文档编辑器完整技术方案.md`  
2. `A-ED-M-T-02_block模型.md`（目录项）  
3. `A-ED-M-T-03_分页系统.md`（目录项）  
4. `A-ED-M-T-04_docx能力.md`（目录项）  
5. `A-WS-M-T-01_workspace架构.md`  
6. `A-DE-M-D-01_对话编辑统一方案.md`  
7. `A-DE-M-T-01_diff系统规则.md`  
8. `A-DE-M-T-02_baseline状态协作.md`  
